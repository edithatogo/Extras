# NoMachine No-Admin Smoke Evidence

- [ ] Silent install path remains user-writable with `/DIR=$dir` behavior
- [ ] Confirm no UAC prompt appears (or explicitly block with reason)
- [ ] Shortcut `NoMachine Enterprise Client` launches
- [ ] Uninstall cleanup scope is constrained to Scoop/user locations
- [ ] No HKLM/service/driver writes observed
- [ ] Evidence artifacts captured:
  - install command
  - launch log
  - uninstall verification output
